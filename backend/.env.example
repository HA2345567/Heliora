# Postgres (Neon, Supabase, or local). Rotate the leaked one before using.
DATABASE_URL="postgresql://user:password@host/db?sslmode=require"

# Server
PORT=4000
NODE_ENV=development
CORS_ORIGIN=http://localhost:5173,https://id-preview--7da12da2-2556-4650-bc2e-e91422b0eaf9.lovable.app

# Gemini (rotate the one shared in chat)
GEMINI_API_KEY=your_gemini_key_here
GEMINI_MODEL=gemini-1.5-flash

# Solana
SOLANA_RPC_URL=https://api.devnet.solana.com
HELIORA_PROGRAM_ID=Helioraxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
